[project]
name = "farafmb"
version = "2024.1"
dependencies = [
    "Django~=4.2.0",
    "Pillow",
    "bleach",
    "Markdown",
    "markdown-checklist",
    "markdown-del-ins",
    "psycopg",
    "python-dotenv",
]

[project.optional-dependencies]
"development" = ["djlint", "coverage"]
